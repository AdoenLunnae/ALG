#ifndef NREINAS_LASVEGAS_HPP
#define NREINAS_LASVEGAS_HPP

#include <vector>

std::vector<int> nReinasLasVegas(const int& n, bool& exito);
std::vector<int> nReinasLasVegasRepetir(const int& n, int& numeroIntentos);

#endif